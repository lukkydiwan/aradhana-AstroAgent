# Core agent framework
langgraph>=0.2.55
langchain-mistralai>=0.2.0
langchain-core>=0.3.0
langchain>=0.3.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
sse-starlette>=2.1.0

# Ephemeris: pure-Python Keplerian implementation (app/tools/ephemeris.py).
# pyswisseph would give <0.01° accuracy but requires MSVC/GCC to compile.
# Install it manually and update birth_chart.py / transits.py if a C compiler is available.

# Geocoding
geopy>=2.4.0
timezonefinder>=6.5.2

# Utilities
python-dotenv>=1.0.0
pydantic>=2.9.0
pytz>=2024.1
aiohttp>=3.10.0
httpx>=0.28.0

# Eval
mistralai>=1.0.0
tabulate>=0.9.0
